import type { AbstractSigner, BrowserProvider, Provider, Signer } from 'ethers';
import type { EthereumAddress, HexString } from '../../types/internalTypes.js';
import type { EIP712TypedData, IBlockchainService } from './IBlockchainService.js';
import type { AbiFragmentTypes, AbiReadFunctionJsonFragment } from './abi.types.js';
export type EthersClient = AbstractSigner | BrowserProvider;
interface EthersAdapter {
    getSigner(): Promise<Signer>;
    getProvider(): Promise<Provider>;
}
export declare function isEthersSigner(object: unknown): object is AbstractSigner<Provider>;
export declare class SignerAdapter implements EthersAdapter {
    private readonly signer;
    constructor(signer: AbstractSigner<Provider>);
    getSigner(): Promise<Signer>;
    getProvider(): Promise<Provider>;
}
export declare function isEthersBrowserProvider(object: unknown): object is BrowserProvider;
export declare class BrowserProviderAdapter implements EthersAdapter {
    private readonly provider;
    constructor(provider: BrowserProvider);
    getSigner(): Promise<Signer>;
    getProvider(): Promise<Provider>;
}
export declare class EthersBlockchainService implements IBlockchainService {
    private static ethersModule;
    private static getEthersModule;
    private readonly adapter;
    constructor(client: EthersClient);
    getChainId(): Promise<number>;
    getAddress(): Promise<EthereumAddress>;
    readContract<T extends AbiReadFunctionJsonFragment>(contractAddress: EthereumAddress, abiFunctionFragment: T, parameters: AbiFragmentTypes<T, 'inputs'>): Promise<AbiFragmentTypes<T, 'outputs'>>;
    signTypedData(data: EIP712TypedData): Promise<HexString>;
}
export {};
//# sourceMappingURL=EthersBlockchainService.d.ts.map