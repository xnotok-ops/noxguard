import type { BaseUrl } from '../../types/internalTypes.js';
import type { IApiService, QueryParameters, Headers, Body, ResponseData } from './IApiService.js';
export declare class ApiService implements IApiService {
    private readonly baseUrl;
    constructor(baseUrl: BaseUrl);
    get({ endpoint, query, headers, timeout, }: {
        endpoint: string;
        query?: QueryParameters;
        headers?: Headers;
        timeout?: number;
    }): Promise<ResponseData>;
    post({ endpoint, query, body, headers, timeout, }: {
        endpoint: string;
        query?: QueryParameters;
        body?: Body;
        headers?: Headers;
        timeout?: number;
    }): Promise<ResponseData>;
}
//# sourceMappingURL=ApiService.d.ts.map