import { IS_VIEWER_ABI } from '../services/blockchain/abis/isViewer.abi.js';
import { eciesDecrypt } from '../utils/ecies.js';
import { decodeValue } from '../utils/encoding.js';
import { isHexString } from '../utils/hex.js';
import { generateRsaKeyPair, exportRsaPublicKey, rsaDecrypt, exportRsaPrivateKey, importRsaPrivateKey, } from '../utils/rsa.js';
import { handleToChainId, handleToSolidityType, } from '../utils/types.js';
import { assertRequiredParams } from '../utils/validators.js';
export async function decrypt({ handle, apiService, blockchainService, storageService, config, }) {
    assertRequiredParams({ handle }, ['handle']);
    const [chainId, userAddress] = await Promise.all([
        blockchainService.getChainId(),
        blockchainService.getAddress(),
    ]);
    const chainIdFromHandle = handleToChainId(handle);
    if (chainIdFromHandle !== chainId) {
        throw new Error(`Handle chainId (${chainIdFromHandle}) does not match connected chainId (${chainId})`);
    }
    const isViewer = await blockchainService.readContract(config.smartContractAddress, IS_VIEWER_ABI, [handle, userAddress]);
    if (!isViewer) {
        throw new Error(`Handle (${handle}) does not exist or user (${userAddress}) is not authorized to decrypt it`);
    }
    const solidityType = handleToSolidityType(handle);
    let authorization;
    let rsaPrivateKey;
    let isFreshDecryptionMaterial = false;
    const storageKey = computeDecryptionMaterialStorageKey({
        userAddress,
        chainId,
        verifyingContract: config.smartContractAddress,
    });
    const storedDecryptionMaterial = await retrieveDecryptionMaterial({
        storageKey,
        storageService,
    });
    if (storedDecryptionMaterial) {
        authorization = storedDecryptionMaterial.authorization;
        rsaPrivateKey = storedDecryptionMaterial.rsaPrivateKey;
    }
    else {
        const decryptionMaterial = await generateDecryptionMaterial({
            userAddress,
            chainId,
            smartContractAddress: config.smartContractAddress,
            blockchainService,
        });
        authorization = decryptionMaterial.authorization;
        rsaPrivateKey = decryptionMaterial.rsaPrivateKey;
        isFreshDecryptionMaterial = true;
    }
    let { status, data } = await apiService.get({
        endpoint: `/v0/secrets/${handle}`,
        headers: {
            Authorization: authorization,
        },
    });
    if (status === 401 && storedDecryptionMaterial) {
        try {
            storageService.removeItem(storageKey);
        }
        catch {
        }
        const decryptionMaterial = await generateDecryptionMaterial({
            userAddress,
            chainId,
            smartContractAddress: config.smartContractAddress,
            blockchainService,
        });
        authorization = decryptionMaterial.authorization;
        rsaPrivateKey = decryptionMaterial.rsaPrivateKey;
        isFreshDecryptionMaterial = true;
        ({ status, data } = await apiService.get({
            endpoint: `/v0/secrets/${handle}`,
            headers: {
                Authorization: authorization,
            },
        }));
    }
    if (status !== 200 ||
        typeof data !== 'object' ||
        data === null ||
        !isHexString(data?.ciphertext) ||
        !isHexString(data?.iv, 12) ||
        !isHexString(data?.encryptedSharedSecret)) {
        throw new Error(`Unexpected response from Handle Gateway (status: ${status}, data: ${JSON.stringify(data)})`);
    }
    const { ciphertext, iv, encryptedSharedSecret } = data;
    if (isFreshDecryptionMaterial) {
        await storeDecryptionMaterial({
            storageKey,
            authorization,
            rsaPrivateKey,
            storageService,
        }).catch(() => {
        });
    }
    const sharedSecret = await rsaDecrypt({
        privateKey: rsaPrivateKey,
        ciphertext: encryptedSharedSecret,
    }).catch((error) => {
        throw new Error('Failed to decrypt shared secret', { cause: error });
    });
    const plaintext = await eciesDecrypt({
        ciphertext,
        iv,
        sharedSecret,
    }).catch((error) => {
        throw new Error('Failed to decrypt ciphertext', { cause: error });
    });
    let value;
    try {
        value = decodeValue(plaintext, solidityType);
    }
    catch (error) {
        throw new Error(`Failed to decode decrypted plaintext: expected hex encoded ${solidityType}, got ${plaintext}`, { cause: error });
    }
    return { value, solidityType };
}
async function generateDecryptionMaterial({ userAddress, chainId, smartContractAddress, blockchainService, }) {
    const rsaKeyPair = await generateRsaKeyPair().catch((error) => {
        throw new Error('Failed to generate RSA key pair', { cause: error });
    });
    const spkiHexRsaPubKey = await exportRsaPublicKey(rsaKeyPair).catch((error) => {
        throw new Error('Failed to export RSA public key', { cause: error });
    });
    const rsaPrivateKey = rsaKeyPair.privateKey;
    const now = Math.floor(Date.now() / 1000);
    const dataAccessAuthorizationTypedData = {
        types: {
            EIP712Domain: [
                {
                    name: 'name',
                    type: 'string',
                },
                {
                    name: 'version',
                    type: 'string',
                },
                {
                    name: 'chainId',
                    type: 'uint256',
                },
                {
                    name: 'verifyingContract',
                    type: 'address',
                },
            ],
            DataAccessAuthorization: [
                {
                    name: 'userAddress',
                    type: 'address',
                },
                {
                    name: 'encryptionPubKey',
                    type: 'string',
                },
                {
                    name: 'notBefore',
                    type: 'uint256',
                },
                {
                    name: 'expiresAt',
                    type: 'uint256',
                },
            ],
        },
        domain: {
            name: 'Handle Gateway',
            version: '1',
            chainId: chainId,
            verifyingContract: smartContractAddress,
        },
        primaryType: 'DataAccessAuthorization',
        message: {
            userAddress: userAddress,
            encryptionPubKey: spkiHexRsaPubKey,
            notBefore: now,
            expiresAt: now + 3600,
        },
    };
    const signature = await blockchainService
        .signTypedData(dataAccessAuthorizationTypedData)
        .catch((error) => {
        throw new Error('Failed to sign data access authorization', {
            cause: error,
        });
    });
    const authorization = `EIP712 ${btoa(JSON.stringify({
        payload: dataAccessAuthorizationTypedData.message,
        signature: signature,
    }))}`;
    return { authorization, rsaPrivateKey };
}
async function retrieveDecryptionMaterial({ storageKey, storageService, }) {
    let data;
    try {
        data = storageService.getItem(storageKey);
    }
    catch {
        return undefined;
    }
    if (data === null) {
        return undefined;
    }
    try {
        const { authorization, pkcs8 } = JSON.parse(data);
        const rsaPrivateKey = await importRsaPrivateKey({ pkcs8Hex: pkcs8 });
        const now = Math.floor(Date.now() / 1000);
        const authPayload = JSON.parse(atob(authorization.split('EIP712 ')[1]));
        if (typeof authPayload.payload.notBefore !== 'number' ||
            now < authPayload.payload.notBefore ||
            typeof authPayload.payload.expiresAt !== 'number' ||
            now > authPayload.payload.expiresAt - 10) {
            throw new Error('Invalid stored authorization');
        }
        return { authorization, rsaPrivateKey };
    }
    catch {
        try {
            storageService.removeItem(storageKey);
        }
        catch {
        }
    }
    return undefined;
}
function computeDecryptionMaterialStorageKey({ userAddress, verifyingContract, chainId, }) {
    const version = '1';
    return `DecryptionMaterial:${userAddress}:${chainId}:${verifyingContract}:${version}`;
}
async function storeDecryptionMaterial({ storageKey, authorization, rsaPrivateKey, storageService, }) {
    try {
        const pkcs8 = await exportRsaPrivateKey({ privateKey: rsaPrivateKey });
        const data = {
            authorization,
            pkcs8,
        };
        storageService.setItem(storageKey, JSON.stringify(data));
    }
    catch (error) {
        throw new Error('Failed to store authorization and RSA private key', {
            cause: error,
        });
    }
}
//# sourceMappingURL=decrypt.js.map