"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.EthersBlockchainService = exports.BrowserProviderAdapter = exports.SignerAdapter = void 0;
exports.isEthersSigner = isEthersSigner;
exports.isEthersBrowserProvider = isEthersBrowserProvider;
const format_js_1 = require("../../utils/format.js");
function isEthersSigner(object) {
    return (!!object &&
        typeof object === 'object' &&
        'getAddress' in object &&
        typeof object.getAddress === 'function' &&
        'provider' in object &&
        !!object.provider &&
        typeof object.provider === 'object' &&
        'getNetwork' in object.provider &&
        typeof object.provider.getNetwork === 'function');
}
class SignerAdapter {
    signer;
    constructor(signer) {
        this.signer = signer;
    }
    async getSigner() {
        return this.signer;
    }
    async getProvider() {
        return this.signer.provider;
    }
}
exports.SignerAdapter = SignerAdapter;
function isEthersBrowserProvider(object) {
    return (!!object &&
        typeof object === 'object' &&
        'getSigner' in object &&
        typeof object.getSigner === 'function' &&
        'getNetwork' in object &&
        typeof object.getNetwork === 'function');
}
class BrowserProviderAdapter {
    provider;
    constructor(provider) {
        this.provider = provider;
    }
    async getSigner() {
        try {
            return await this.provider.getSigner();
        }
        catch (error) {
            throw new Error('Failed to get signer from BrowserProvider', {
                cause: error,
            });
        }
    }
    async getProvider() {
        return this.provider;
    }
}
exports.BrowserProviderAdapter = BrowserProviderAdapter;
class EthersBlockchainService {
    static ethersModule = null;
    static async getEthersModule() {
        this.ethersModule ??= await Promise.resolve().then(() => __importStar(require('ethers')));
        return this.ethersModule;
    }
    adapter;
    constructor(client) {
        if (isEthersSigner(client)) {
            this.adapter = new SignerAdapter(client);
        }
        else if (isEthersBrowserProvider(client)) {
            this.adapter = new BrowserProviderAdapter(client);
        }
        else {
            throw new TypeError('Unsupported client. Expected an ethers AbstractSigner instance connected to a Provider or a BrowserProvider.');
        }
    }
    async getChainId() {
        try {
            const provider = await this.adapter.getProvider();
            const network = await provider.getNetwork();
            return Number(network.chainId);
        }
        catch (error) {
            throw new Error('Failed to get chain ID', { cause: error });
        }
    }
    async getAddress() {
        try {
            const signer = await this.adapter.getSigner();
            return (await signer.getAddress());
        }
        catch (error) {
            throw new Error('Failed to get address', { cause: error });
        }
    }
    async readContract(contractAddress, abiFunctionFragment, parameters) {
        try {
            const provider = await this.adapter.getProvider();
            const { Contract } = await EthersBlockchainService.getEthersModule();
            const contract = new Contract(contractAddress, [abiFunctionFragment], provider);
            const method = contract[abiFunctionFragment.name];
            return await method(...parameters);
        }
        catch (error) {
            throw new Error(`Failed to read contract at ${contractAddress} (method: ${abiFunctionFragment.name}, parameters: ${(0, format_js_1.safeJsonStringify)(parameters)})`, {
                cause: error,
            });
        }
    }
    async signTypedData(data) {
        try {
            const signer = await this.adapter.getSigner();
            const { EIP712Domain, ...types } = data.types;
            return (await signer.signTypedData(data.domain, types, data.message));
        }
        catch (error) {
            throw new Error('Failed to sign typed data', { cause: error });
        }
    }
}
exports.EthersBlockchainService = EthersBlockchainService;
//# sourceMappingURL=EthersBlockchainService.js.map