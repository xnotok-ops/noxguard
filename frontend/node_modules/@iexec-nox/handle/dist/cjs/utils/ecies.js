"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.eciesDecrypt = eciesDecrypt;
const hex_js_1 = require("./hex.js");
const DERIVATION_INFO = (0, hex_js_1.hexToBytes)('0x45434945533a4145535f47434d3a7631');
async function eciesDecrypt({ ciphertext, iv, sharedSecret, }) {
    const ciphertextBytes = (0, hex_js_1.hexToBytes)(ciphertext);
    const sharedSecretBytes = (0, hex_js_1.hexToBytes)(sharedSecret);
    const ivBytes = (0, hex_js_1.hexToBytes)(iv);
    if (sharedSecretBytes.length !== 32) {
        throw new TypeError('Invalid shared secret length');
    }
    if (ivBytes.length !== 12) {
        throw new TypeError('Invalid IV length');
    }
    const keyMaterial = await crypto.subtle.importKey('raw', sharedSecretBytes, 'HKDF', false, ['deriveKey']);
    const aesKey = await crypto.subtle.deriveKey({
        name: 'HKDF',
        hash: 'SHA-256',
        salt: new Uint8Array(32),
        info: DERIVATION_INFO,
    }, keyMaterial, { name: 'AES-GCM', length: 256 }, false, ['decrypt']);
    const decryptedBytes = await crypto.subtle.decrypt({
        name: 'AES-GCM',
        iv: ivBytes,
        tagLength: 128,
    }, aesKey, ciphertextBytes);
    return (0, hex_js_1.bytesToHex)(new Uint8Array(decryptedBytes));
}
//# sourceMappingURL=ecies.js.map